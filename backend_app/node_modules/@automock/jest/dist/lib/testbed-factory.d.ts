import { TestBedResolver } from './test-bed-resolver';
import { Type } from './types';
export declare class TestBed {
    /**
     * Create new unit builder
     * @param targetClass
     * @return TestBedResolver
     */
    static create<TClass = any>(targetClass: Type<TClass>): TestBedResolver<TClass>;
}
